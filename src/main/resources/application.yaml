spring:
  application:
    name: ShareAll

  datasource:
    url: jdbc:postgresql://postgres:5432/share_all_db
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
